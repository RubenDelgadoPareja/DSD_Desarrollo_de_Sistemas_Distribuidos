/*
 * Please do not edit this file.
 * It was generated using rpcgen.
 */

#include "ServicioBasicoDeRegistro.h"
#include <stdio.h>
#include <stdlib.h>
#include <rpc/pmap_clnt.h>
#include <string.h>
#include <memory.h>
#include <sys/socket.h>
#include <netinet/in.h>

#ifndef SIG_PF
#define SIG_PF void(*)(int)
#endif

static char **
_registra_1 (char * *argp, struct svc_req *rqstp)
{
	return (registra_1_svc(*argp, rqstp));
}

static int *
_borra_1 (char * *argp, struct svc_req *rqstp)
{
	return (borra_1_svc(*argp, rqstp));
}

static char **
_buscaip_1 (char * *argp, struct svc_req *rqstp)
{
	return (buscaip_1_svc(*argp, rqstp));
}

static char **
_visualizartabladns_1 (void  *argp, struct svc_req *rqstp)
{
	return (visualizartabladns_1_svc(rqstp));
}

static char **
_vtabla_1 (char * *argp, struct svc_req *rqstp)
{
	return (vtabla_1_svc(*argp, rqstp));
}

static void
serviciobasicoderegistro_1(struct svc_req *rqstp, register SVCXPRT *transp)
{
	union {
		char *registra_1_arg;
		char *borra_1_arg;
		char *buscaip_1_arg;
		char *vtabla_1_arg;
	} argument;
	char *result;
	xdrproc_t _xdr_argument, _xdr_result;
	char *(*local)(char *, struct svc_req *);

	switch (rqstp->rq_proc) {
	case NULLPROC:
		(void) svc_sendreply (transp, (xdrproc_t) xdr_void, (char *)NULL);
		return;

	case REGISTRA:
		_xdr_argument = (xdrproc_t) xdr_wrapstring;
		_xdr_result = (xdrproc_t) xdr_wrapstring;
		local = (char *(*)(char *, struct svc_req *)) _registra_1;
		break;

	case BORRA:
		_xdr_argument = (xdrproc_t) xdr_wrapstring;
		_xdr_result = (xdrproc_t) xdr_int;
		local = (char *(*)(char *, struct svc_req *)) _borra_1;
		break;

	case BUSCAIP:
		_xdr_argument = (xdrproc_t) xdr_wrapstring;
		_xdr_result = (xdrproc_t) xdr_wrapstring;
		local = (char *(*)(char *, struct svc_req *)) _buscaip_1;
		break;

	case VISUALIZARTABLADNS:
		_xdr_argument = (xdrproc_t) xdr_void;
		_xdr_result = (xdrproc_t) xdr_wrapstring;
		local = (char *(*)(char *, struct svc_req *)) _visualizartabladns_1;
		break;

	case VTABLA:
		_xdr_argument = (xdrproc_t) xdr_wrapstring;
		_xdr_result = (xdrproc_t) xdr_wrapstring;
		local = (char *(*)(char *, struct svc_req *)) _vtabla_1;
		break;

	default:
		svcerr_noproc (transp);
		return;
	}
	memset ((char *)&argument, 0, sizeof (argument));
	if (!svc_getargs (transp, (xdrproc_t) _xdr_argument, (caddr_t) &argument)) {
		svcerr_decode (transp);
		return;
	}
	result = (*local)((char *)&argument, rqstp);
	if (result != NULL && !svc_sendreply(transp, (xdrproc_t) _xdr_result, result)) {
		svcerr_systemerr (transp);
	}
	if (!svc_freeargs (transp, (xdrproc_t) _xdr_argument, (caddr_t) &argument)) {
		fprintf (stderr, "%s", "unable to free arguments");
		exit (1);
	}
	return;
}

int
main (int argc, char **argv)
{
	register SVCXPRT *transp;

	pmap_unset (SERVICIOBASICODEREGISTRO, DNS);

	transp = svcudp_create(RPC_ANYSOCK);
	if (transp == NULL) {
		fprintf (stderr, "%s", "cannot create udp service.");
		exit(1);
	}
	if (!svc_register(transp, SERVICIOBASICODEREGISTRO, DNS, serviciobasicoderegistro_1, IPPROTO_UDP)) {
		fprintf (stderr, "%s", "unable to register (SERVICIOBASICODEREGISTRO, DNS, udp).");
		exit(1);
	}

	transp = svctcp_create(RPC_ANYSOCK, 0, 0);
	if (transp == NULL) {
		fprintf (stderr, "%s", "cannot create tcp service.");
		exit(1);
	}
	if (!svc_register(transp, SERVICIOBASICODEREGISTRO, DNS, serviciobasicoderegistro_1, IPPROTO_TCP)) {
		fprintf (stderr, "%s", "unable to register (SERVICIOBASICODEREGISTRO, DNS, tcp).");
		exit(1);
	}

	svc_run ();
	fprintf (stderr, "%s", "svc_run returned");
	exit (1);
	/* NOTREACHED */
}
